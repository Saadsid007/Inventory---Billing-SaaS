{
  "name": "billwise",
  "version": "0.0.0",
  "private": true,
  "description": "Multi-tenant inventory + billing SaaS for Indian small businesses, with a public product catalog per business.",
  "packageManager": "pnpm@11.18.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "format": "prettier --write \"**/*.{ts,tsx,mjs,json,md,css,yaml,yml}\"",
    "format:check": "prettier --check \"**/*.{ts,tsx,mjs,json,md,css,yaml,yml}\"",
    "db:generate": "pnpm --filter @billwise/db run db:generate",
    "db:migrate": "pnpm --filter @billwise/db run db:migrate",
    "db:studio": "pnpm --filter @billwise/db run db:studio",
    "clean": "turbo run clean",
    "db:seed": "pnpm --filter @billwise/db run db:seed",
    "seed:admin": "pnpm --filter @billwise/web seed:admin",
    "test:integration": "pnpm --filter @billwise/db run test:integration"
  },
  "devDependencies": {
    "@eslint/js": "catalog:",
    "eslint": "catalog:",
    "globals": "catalog:",
    "prettier": "catalog:",
    "turbo": "^2.10.12",
    "typescript": "catalog:",
    "typescript-eslint": "catalog:"
  }
}
