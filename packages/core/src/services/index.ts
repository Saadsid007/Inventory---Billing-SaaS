// Orchestration: createInvoice, issueInvoice, cancelInvoice — build spec §2.5.
// Filled in during Phase 1d. These take plain data and a set of injected
// operations; they never import a repository directly.
export {};
