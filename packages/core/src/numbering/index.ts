// Gapless invoice numbering — build spec §5.1. Filled in at build-order step 6.
// The pure half only: series formatting and FY derivation. The row lock lives
// in packages/db, because locking is a database concern.
export {};
